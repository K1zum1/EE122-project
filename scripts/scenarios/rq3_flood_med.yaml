meta:
  id: rq3_flood_med
  rq: 3
  description: Flood 1000 pps, detect_mitigate
overrides:
  attack:
    duration: 30
    warmup_s: 3
    enabled: true
    mode: constant
    target_pps: 1000
  benign:
    enabled: true
    pps: 50
    payload_size: 64
  probe:
    enabled: true
    pps: 50
    payload_size: 32
  sdn:
    defense_mode: detect_mitigate
  spoof:
    enabled: false
