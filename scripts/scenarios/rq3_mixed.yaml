meta:
  id: rq3_mixed
  rq: 3
  description: Mixed flood + spoof, detect_mitigate
overrides:
  attack:
    duration: 30
    warmup_s: 3
    enabled: true
    mode: constant
    target_pps: 1000
  benign:
    enabled: true
    pps: 50
    payload_size: 64
  probe:
    enabled: true
    pps: 50
    payload_size: 32
  sdn:
    defense_mode: detect_mitigate
  spoof:
    enabled: true
    pps: 100
    many_identities: false
    num_identities: 20
